version: "3.9"

services:
  frontend:
    container_name: ${VITE_APP_NAME:-meduzzen-frontend}
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${SERVER_PORT:-3000}:80"
    env_file:
      - .env
    restart: unless-stopped
